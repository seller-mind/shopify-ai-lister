node_modules
.build
.vercel
.shopify
.env
.env.*
!.env.example
/cache
public/build
build
*.tsbuildinfo
.cache/

# App Store listing submission docs — contain test store credentials,
# Client Secret, and other materials only meant for Shopify reviewer.
# NEVER commit to public/shared repo.
app-store-listing/
